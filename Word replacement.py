def replace_word():

    str = "My favourite colour is Pink!"
    word_to_replace = input("Enter the word to replace:")
    word_to_replace_with = input("Enter the word to be replaced with:")

    print(str.replace(word_to_replace,word_to_replace_with))


replace_word()